/// Unwraps a proxy URL to extract the original URL, if applicable.
pub fn unwrap_proxy_url(url: &str) -> Option<String> {
    // Stub: no proxy transformation in standalone app
    let _ = url;
    None
}
